/** @typedef {import("../userProfile/user").User} User */

/**
 * @typedef {Object} SettingsPageProps
 * @property {User} user
 * @property {(updatedUser: User) => void} onUpdateUser
 * @property {() => void} onBack
 */

export {};
