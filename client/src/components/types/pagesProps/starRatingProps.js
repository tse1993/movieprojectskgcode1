/**
 * @typedef {Object} StarRatingProps
 * @property {string} movieId
 * @property {number} [initialRating]
 * @property {(movieId: string, rating: number) => void} onRate
 * @property {string} [className]
 */

export {};
