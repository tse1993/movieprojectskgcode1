/**
 * @typedef {Object} StarRatingViewProps
 * @property {number} currentRating
 * @property {number} hoveredRating
 * @property {(rating:number)=>void} onStarClick
 * @property {(rating:number)=>void} onStarHover
 * @property {()=>void} onMouseLeave
 * @property {string} [className]
 */

export {};